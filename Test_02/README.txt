Test_02

Include a directories to compile using:
	- include_directories() 

The include_directories(${PROJECT_SOURCE/include}) add the include directory to the include search path os the compiler to all target defined in this directory (and all subdirectories included via add_directory())


